services:

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: charge_flow_backend
    command: sleep infinity
    env_file:
      - .env
    environment:
      - TZ=Europe/Moscow
